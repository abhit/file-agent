Agent packaged in a container to read through files on a local share. The agent can also run web based queries as well. 
The agent is configured to leverage GPU based inference.
The LLM can be chosen by the user through the web-based UI using OLLAMA.
